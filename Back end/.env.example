# Copiá este archivo a .env y completá los valores.
# En la VM o PC donde corra Docker: cp .env.example .env  (o copy en Windows)
# NO subas .env a Git (tiene secretos).

NODE_ENV=production
PORT=4000

# MongoDB: en Docker el compose ya pone MONGODB_URI. Si corrés sin Docker:
# MONGODB_URI=mongodb://localhost:27017/lavadero

# JWT: generá una clave larga (ej: openssl rand -hex 32)
JWT_SECRET=cambiar-por-una-clave-secreta-muy-larga-y-aleatoria

# Email (Gmail: usar "Contraseña de aplicación", no la contraseña normal)
EMAIL_SERVICE=gmail
EMAIL_USER=tu-email@gmail.com
EMAIL_PASS=tu-contraseña-de-aplicacion
EMAIL_DESTINO=lavaderojmm@gmail.com

# RabbitMQ: en Docker el compose ya pone RABBITMQ_URL. Si corrés sin Docker:
# RABBITMQ_URL=amqp://localhost:5672
